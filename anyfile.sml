val x = 34;
val y = 17;
val z = (x + y) ;
val q = 56;
val k = q + 9;


(* 5 > 6 should be a bool value 
  if it's true then `then` value will
  get considered otherwise else value
  the value inside the then and else should be of same type.
*)
val x = if 5 > 6 then 8 else 9;

